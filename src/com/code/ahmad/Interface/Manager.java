package com.code.ahmad.Interface;

public interface Manager extends Ceo{

	/* one more interface with additional method anyone 
	 * implements this interface will have to implement
	 * all the methods in Manger and ceo interface
	 */
	public void getTask();
	
}
